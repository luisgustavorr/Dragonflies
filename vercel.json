{
    "version":2,
    "builds":[{
        "src":"websocket-server.js",
        "use":"@now/node"
    }],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"websocket-server.js"
        }
    ]
}